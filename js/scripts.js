// ADDRESS BOOK

function Contact(first, last) {
  this.firstName = first;
  this.lastName = last;
}
var ada = new Contact("Ada", "Lovelace");


// Business (or back-end) logic:


// Everything below this line is user interface (or front-end) logic:

$(document).ready(function() {

    event.preventDefault();
});


var testGreeting = newString;
var testGreeting2 = new String("Hello!");

//constructor function
function Dog(name, colors, age) {
  this.name = name;
  this.colors = colors;
  this.age = age;
}

// my puppy is an 'instance' of the 'Dog' type
var myPuppy - new Dog("Ernie", ["brown", "black"], 3);
myPuppy.name;
myPuppy.colors;

//prototypes
String.prototype.addExcitement = function() {return this + "!!!!"};
Dog.prototype.speak = function() {
  console.log("Woof!");
}
